个人使用的APP。没有安全方面的考虑，纯粹玩玩，但是我认为有创意

采用百度地图API，编译时需要更改 
另外由于受国内手机限制，无法使用google cloud message，转而使用个推API，也可以设置。
这个APP还需要与服务器配合，当然也能实现P2P，可惜个推的API太烂，我都不好意思说为什么不用P2P。
当然用服务器也有好处，可以记录有趣的事。
主要功能:
两个小伙伴，分别把对方填入跟踪对象，然后就可以实时跟踪了。
其他功能以后再加
